import { sourceDescriptions } from '../Data/news'
import SectionTitle from './SectionTitle'
export default function SourceCards() { return <section className="sources-section" id="quellen"><SectionTitle>Unsere Quellen</SectionTitle><p className="section-intro">Fünf Perspektiven, um ein Land zu verstehen</p><div className="source-grid">{sourceDescriptions.map(source => <a className="source-card" key={source.name} href={source.url} target="_blank" rel="noopener noreferrer" style={{ '--source-color': source.tint } as React.CSSProperties}><div className="source-seal"><span>{source.mark}</span></div><h3>{source.name}</h3><p>{source.detail}</p><span className="source-card__link">Nachrichten ansehen <span>→</span></span></a>)}</div></section> }
