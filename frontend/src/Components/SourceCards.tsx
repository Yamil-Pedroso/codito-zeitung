import { sourceDescriptions } from '../Data/news'
import SectionTitle from './SectionTitle'
export default function SourceCards() { return <section className="sources-section" id="quellen"><SectionTitle>Unsere Quellen</SectionTitle><p className="section-intro">Fünf Perspektiven, um ein Land zu verstehen</p><div className="source-grid">{sourceDescriptions.map(source => <article className="source-card" key={source.name} style={{ '--source-color': source.tint } as React.CSSProperties}><div className="source-seal"><span>{source.mark}</span></div><h3>{source.name}</h3><p>{source.detail}</p><a href={`#${source.mark.toLowerCase()}`}>Nachrichten ansehen <span>→</span></a></article>)}</div></section> }
